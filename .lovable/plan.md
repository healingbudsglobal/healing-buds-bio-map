

## Plan: Create Admin Account

**Goal**: Create the admin user account with email `healingbudsglobal@gmail.com` and the password you provided, using the `create-admin` backend function.

### Steps

1. **Invoke the `create-admin` backend function** with the email and password to securely create the admin user with email auto-confirmed
2. **Verify** the account was created successfully
3. **Test** login at `/admin/login` to confirm access to the dashboard

This is a single backend call — no code changes needed.

